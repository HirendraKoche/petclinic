version: 2.0
jobs:
    build:
        docker:
            - image: maven:latest

        steps:
            - checkout
            - run:
                name: Build code
                command:
                    mvn clean package
